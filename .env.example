# Copy to .env (gitignored) and fill in. Never commit the real file.
#
# A registered Pokemon Showdown account for `scripts/ladder_live.py`. Register
# the name on play.pokemonshowdown.com first; an unregistered name can log in
# but its rating is not kept. Name it so opponents can tell it is a bot.
PS_USERNAME=
PS_PASSWORD=
