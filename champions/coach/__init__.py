"""The coach: a finished game re-solved offline, two losses per turn.

`docs/06-coach-and-evaluation.md` part I, built to
`docs/specs/2026-09-13-coach.md`. `decisions` turns a trace or a replay into
one shape, `analyze` produces the numbers, `classify` holds the rule,
`explain` writes it up, `report` emits the overlay and the document.
"""
